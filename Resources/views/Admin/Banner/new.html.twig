{% set main_menu = 'banner' %}
{% set width = '70%' %}
{% extends app.request.query.get('isModal')|default(false) ? "NfqAdminBundle::modal.html.twig" : "NfqAdminBundle::layout.html.twig" %}

{% block title %}
    {{ 'banner.banner_new'|trans }}
{% endblock %}

{% block content %}
    {{ tinymce_init() }}
    <div class="row">
        <div class="col-lg-12">
            <ul class="nav nav-tabs language-tabs" role="tablist">
                {% for locale, form in forms %}
                    <li class="{% if isActiveLocale(_context, locale) %}active{% endif %}"><a href="#{{ locale }}-tab" role="tab" data-toggle="tab">{{ locale }}</a></li>
                {% endfor %}
            </ul>
            <br />
            <div class="tab-content">
                {% embed 'NfqBannerBundle:Admin/Banner:_form.htm.twig' %}{% endembed %}
            </div>
        </div>
    </div>
{% endblock %}